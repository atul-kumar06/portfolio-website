import React from "react";
import Nav from "../components/Nav";
import StartBackground from "../features/StartBackground";

const Home = () => {
  return (
    <div>
      <StartBackground />
      <Nav />
    </div>
  );
};

export default Home;
